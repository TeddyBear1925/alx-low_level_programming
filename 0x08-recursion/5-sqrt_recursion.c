#include "main.h"

/**
 * _sqrt_recursion - returns the natural square root of a number
 * @n: number to be calculated
 * Return: the results
 */

int _sqrt_recursion(int n)
{
if (n < 0)
return (-1);
return (actual_sqrt_recursi0n(n, 0));

