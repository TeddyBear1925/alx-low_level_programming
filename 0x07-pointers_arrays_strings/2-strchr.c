#include "main.h"

/**
 * _memcpy - function that copies memory area
 * @s: input
 * @c: input
 * Return: 0 (success)
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
int i = 0;
for (; s[i] >= '\0'; i++)
{
if (s[i] == c )
return (&s[i]);
}
return (0);
}
